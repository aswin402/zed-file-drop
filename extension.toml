id          = "zed-file-drop"
name        = "File Drop"
description = "Paste images from your clipboard directly into Zed as markdown image links."
version     = "0.2.0"
schema_version = 1
authors     = ["you <you@example.com>"]
repository  = "https://github.com/your-name/zed-file-drop"

[slash_commands.paste-image]
description     = "Paste an image from the clipboard into the current file (saves to assets/)"
requires_argument = false